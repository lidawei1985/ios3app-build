# RULES · 本项目必须遵守的工程规则

> **唯一真源**：`F:\2026-09-16-03-24-27\工程规则总纲.md`
>
> 进入本项目动手前必读《工程规则总纲》（ENGRULES）。核心红线：
> 1) 先跑 `arc.py report` 做身份核验，防改错项目；
> 2) 项目不串线，不跨项目复制代码/配置/数据/版本号；
> 3) 坑要变成三层资产：记录 → 检测 → 规则，只记录算未完成；
> 4) 稳定事实（IP/路径/已盘点）先查缓存，禁止重复扫描浪费时间/积分；
> 5) 删除变量/文件前先证无引用 + 备份；红线内禁止删；
> 6) 一个项目只维护 **`PROJECT.md`** 一份 MD，不堆多份报告；
> 7) 用户要求"改"的指令逐条登记进 PROJECT.md「变更台账」，没做=待办，做了=回填证据；
> 8) 换账号/换会话接手：先读 PROJECT.md「交接簿」最新一条 → arc.py report → 按"下一步"继续；
>    干活时每完成一步实时更新交接簿，禁止只存聊天/云端记忆；
> 9) 只做用户要求的事，禁止顺手加功能/重构/美化；用户说"不需要X"=永久负向红线（登记+机检）；
>    要求修的东西必须在台账闭环，修不完要明说，禁止无声消失；
> 10) 构建产物交付前过「交付卫生纪律」：清无用代码/调试残留/垃圾/历史版本堆积，只留一份现行源码；
>    grep 证无引用+备份后再删，清理记录写入 PROJECT.md，回归 PASS 才算交付；
> 11) 最小改动：diff 每一行都对得上用户指令，一行能修的不写一堆，不引新依赖不改无关文件；
>    上下文节俭：回复不灌水、引用不复制；独立操作并行、已知事实复用；验证一步不省。
